-- jump (lazy only)
function jmp(to)
    __Anita.advancedDialogueHelper:Jump(to)
end

-- override next dialogue text (lazy only)
function override_text(to)
    __Anita.advancedDialogueHelper:Override(to)
end
